function closedPaths(number) {
  // Write your code here
  let n = 649578;
  let number = 0;
  while (n != 0) {
    if (ch == 0 || ch == 4 || ch == 6 || ch == 9) {
      number = number + 1;
    }
    if (ch == 8) {
      number = number + 2;
    }
    let ch = n / 10;
  }
}
closedPaths(sum);
